"""Phase 3 — verdict report generation (brief §7). NOT YET IMPLEMENTED.

Markdown report per analyzed window: verdict class + confidence + summary,
decomposition table vs adjusted baseline with sigma, evidence ledger
(matched AND rejected signals with quantified reasons), explicit
"what it is NOT" section, and recommended action.
"""

raise NotImplementedError("Phase 3 — implement after correlator")
